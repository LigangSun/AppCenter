<UserControl x:Class="SoonLearning.Assessment.Player.UserControls.FIBQuestionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Foreground="{StaticResource TextColorBrush_Light}"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             Initialized="UserControl_Initialized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--<Label x:Name="questionBodyLabel" 
               Margin="0,0,0,0"
               HorizontalAlignment="Center"
               FontSize="30"/>-->
        
        <StackPanel x:Name="questionBodyPanel"
                    Margin="0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"/>

        <WrapPanel x:Name="blankPanel"
                   MaxWidth="520"
                   Margin="10"
                   Grid.Row="1" />

    </Grid>
</UserControl>
