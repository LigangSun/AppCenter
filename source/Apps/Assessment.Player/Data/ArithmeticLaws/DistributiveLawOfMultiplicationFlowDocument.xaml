<FlowDocument xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              ColumnWidth="400" FontSize="30" FontFamily="宋体"
              Foreground="Black">
    <Paragraph>
        <Run FontSize="36">定义：</Run>
    </Paragraph>
    <Section>
        <Paragraph>
            两个数的和同一个数相乘，可以把两个加数分别同这个数相乘，再把两个积相加，结果不变。如：
            (a+b)×c=a×c+b×c
        </Paragraph>
    </Section>

    <Paragraph>
        <Run FontSize="36">举例：</Run>
    </Paragraph>
    <Paragraph>
        (2+5)×3=2×3+5×3，在这个等式中，先2加5，和再乘以3；和2乘以3，5乘以3，再相加，结果不变，都是21。
    </Paragraph>
</FlowDocument>
